{
  "version": "0.1.0",
  "connector": "teams_bot",
  "connector_internals": [],
  "valid_action_names": [
    "get_user_by_principal_name",
    "post_blocks_message"
  ],
  "valid_trigger_names": [
    "bot_command",
    "help_event",
    "new_event",
    "new_message_event"
  ],
  "action_rules": [],
  "_notes": {
    "verification_status": "bot_command, get_user_by_principal_name, and post_blocks_message confirmed in 5+ production recipes in a live Workato workspace, and re-confirmed by activation-testing a scratch recipe (wk recipes start). help_event confirmed by a human tester end-to-end: guessed name failed in the editor, tester located the real name via the editor's connector/trigger picker, corrected it, then fully activated the recipe, sent 'help' to the live bot in Teams, and confirmed the reply. new_message_event and new_event were both confirmed by the same tester directly selecting them from the connector's own trigger picker in the recipe editor (not activation-fired, but the picker only lists real triggers, and for new_event the editor additionally resolved and returned a full live extended_output_schema for the selected event type -- a materially stronger signal than a name merely accepting import).",
    "get_user_by_principal_name": "Observed with two different input fields across different recipes: `principal_name` (email/UPN) in one recipe, `id` (Azure AD object ID, typically sourced from the bot_command trigger's context.from.aadObjectId) in three others. Both are real, live-verified inputs to the same action -- likely alternate lookup modes on one field, but no toggleCfg was captured in any of the source recipes to confirm that formally.",
    "help_event": "Trigger takes no input (confirmed: the editor shows 'no further setup needed'). context.from.aadObjectId -- the exact same path used on bot_command -- resolves and works identically here, confirmed by a full activation test: get_user_by_principal_name(id: context.from.aadObjectId) -> post_blocks_message(channel: that id) round-tripped a real reply in Teams. IMPORTANT naming-convention lesson: Workato's internal trigger name does NOT always match the doc's UI label in an obvious snake_case way -- 'New help message trigger' in the docs is `help_event` internally, not `new_help_message`. Do not assume any other still-unverified name below follows a predictable pattern from its doc label either.",
    "new_message_event": "Real internal name for the doc's 'New message trigger (beta)' -- another case of the doc label not matching the internal name (guessed `new_message`, actual is `new_message_event`). Confirmed via the editor's trigger picker (picker label: 'New message', description 'Triggers when a new text message is sent to Workbot'). Supports a trigger condition (`filter` block), matching the doc's optional channel-scoping feature. Output field paths (e.g. `message_text`, `conversation_id`) used elsewhere in this skill are still GUESSES modeled on the doc's field list, not yet confirmed against a live extended_output_schema -- do not treat them as verified the way help_event's or bot_command's context path is.",
    "new_event": "A single generic, polymorphic real-time-event trigger (picker label: 'New real-time event', description 'Triggers when selected event occurs in Microsoft Teams') -- NOT a family of separate triggers, one per event. Its `event_name` input is a live-search picklist (not a static enum); only 'Typeahead search' (`event_name` value `application/search`) returned a match in this workspace when searched, including with the terms tab/task/card/fetch/adaptive/compose. The Typeahead search event's schema was fully captured live (see SKILL.md / patterns/real-time-events.md): a top-level `from.aadObjectId` (note: top-level, NOT nested under `context` like bot_command/help_event), plus `conversation`, `channelData`, and a `value.queryText`/`queryOptions`/`dataset` object for the search request itself. Because no tab- or task-module-related event name surfaced in this connector instance, Tab opened trigger and Show tab using Adaptive Cards action (from Workato's own docs) appear to NOT be available on this bot at all, likely because those require an Enterprise Workbot tier/manifest (per the docs' own 'Enterprise Workbot' gating for tabs/files) that this connection isn't provisioned with -- this is a checked, live finding, not merely an unverified gap.",
    "bot_command_parameters": "Parameter `type`/`control_type` pairs confirmed in production: string/text, and date_time/date (with parse_output/render_input: date_conversion on the corresponding extended_output_schema entry). The `file` type mentioned in Workato's own Workbot-for-Teams documentation has not been observed in any recipe in this workspace and is NOT included here or assumed to work.",
    "post_blocks_message_blocks": "text_block's text_type has been observed as both body_text and custom (the latter paired with separator/style.isSubtle for a de-emphasized footer line). text_with_button_block's button_type has only been observed as submit. Neither of these is a new action/trigger name -- see SKILL.md's Block Types Reference for details.",
    "workbotguide_gap": "Workato's own Workbot-for-Teams documentation (docs.workato.com) additionally describes a Delete message action, a Show tab using Adaptive Cards action, and a generic custom action for Microsoft Graph API calls. The connector's own action picker (browsed live in the editor) confirms Delete message, Post message, Post message (old version), Post reply, Post reply (old version), Post simple message, Post simple reply, Response to real-time event, and Custom action all exist as real, selectable actions -- stronger evidence than the doc alone -- but none of their internal `name` fields have been captured yet (requires selecting each in the editor and saving, same as was done for the triggers above). post_blocks_message (already verified via production usage) is presumed to correspond to the UI's 'Post message' label given it always carries a channel/recipient field, but this is NOT yet confirmed, and 'Post reply' likely maps to a distinct, still-unknown internal action (no channel field expected, since a reply implicitly targets the invoking context). Tab opened trigger is very likely NOT a separate trigger at all -- see the new_event note above. Left unverified rather than guessed; test recipes are sitting in the workspace's [RIP] Recipe Improvement Project folder awaiting the same editor-driven capture.",
    "unverified": "No other action or trigger name has been verified against a live connector. Do not add names here from vendor API docs or general knowledge alone."
  }
}
